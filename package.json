{
  "name": "killthenoise-frontend-root",
  "version": "1.0.0",
  "description": "Root package for KillTheNoise frontend deployment",
  "private": true,
  "scripts": {
    "install": "cd client && npm install",
    "build": "cd client && npm run build",
    "start": "cd client && npm start"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
